node_modules/

# Sortie du build Vite (regénérée par npm run dev / npm run build) : c'est le
# publicDir servi par webflow.json, entièrement généré sur cette branche.
public/

# Bundle d'upload
bundle.zip
